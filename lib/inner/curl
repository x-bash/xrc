

___X_CMD_INNER_CURL_BINPATH=

___x_cmd_inner_awk(){
    # GCURL Better Than CURL
    if [ -n "$___X_CMD_INNER_CURL_BINPATH" ]; then
        "$___X_CMD_INNER_CURL_BINPATH" "$@"
        return
    fi

    local ___X_CMD_INNER_CURL_BINPATH
    ___X_CMD_INNER_CURL_BINPATH="$(command -v curl)"
}
